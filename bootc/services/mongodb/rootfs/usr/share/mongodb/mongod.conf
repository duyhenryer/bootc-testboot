# MongoDB configuration (immutable -- shipped in /usr/share/, read-only at runtime)
# Data and logs persist in /var/ across upgrades and rollbacks.
#
# Reference: https://www.mongodb.com/docs/manual/reference/configuration-options/

storage:
  dbPath: /var/lib/mongodb

systemLog:
  destination: file
  path: /var/log/mongodb/mongod.log
  logAppend: true

net:
  port: 27017
  bindIp: 127.0.0.1

processManagement:
  timeZoneInfo: /usr/share/zoneinfo
