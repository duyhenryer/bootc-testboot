# System user for running application services
# Duplicated from base/rootfs/ to survive overlay layer merges in the app image.
g appuser -
u appuser - "Application User" /var/home/appuser /bin/bash
m appuser wheel
