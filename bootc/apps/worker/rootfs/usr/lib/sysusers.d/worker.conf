# Static user for worker service.
# Member of apps group for access to shared resources
# (environment files under /var/lib/bootc-testboot/shared/).
u worker - "worker service" /var/lib/bootc-testboot/worker /usr/sbin/nologin
m worker apps
