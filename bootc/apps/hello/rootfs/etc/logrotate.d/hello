# hello.service: app + healthcheck logs (User=hello; fd kept open — use copytruncate)
/var/log/bootc-testboot/hello/*.log {
    daily
    # weekly
    rotate 28
    copytruncate
    missingok
    notifempty
    su hello hello

    # maxsize 50M
    # size 100M
}
