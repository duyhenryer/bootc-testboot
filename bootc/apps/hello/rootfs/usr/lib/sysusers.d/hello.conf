# Static user for hello service (replaces DynamicUser=yes so logrotate
# can 'su hello hello' even when the service is stopped).
u hello - "hello service" /var/lib/bootc-testboot/hello /usr/sbin/nologin
