# Static user for hello service.
# Member of bootc-apps group for access to shared resources
# (TLS CA certs, environment files under /var/lib/bootc-testboot/shared/).
u hello - "hello service" /var/lib/bootc-testboot/hello /usr/sbin/nologin
m hello bootc-apps
