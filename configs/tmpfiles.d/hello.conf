# Additional /var directories for hello service.
# StateDirectory= and LogsDirectory= in hello.service handle
# /var/lib/hello and /var/log/hello automatically.
# Add extra entries here if the app needs more directories.
#
# Example:
# d /var/lib/hello/uploads 0750 - - -
